apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    app: frontend
    tier: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30030
      name: http