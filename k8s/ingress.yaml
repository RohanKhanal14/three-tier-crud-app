apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: three-tier-crud-app-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: myapp.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-svc
                port:
                  number: 80
